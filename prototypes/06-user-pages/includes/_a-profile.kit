<div class="profile card">
	<img src="{{user.avatar_url || user.gh_data.avatar_url}}" />
	<div class="content">
		<strong>{{user.name}}</strong>
		<br />@{{user.login}}
		<br />{{user.location}}		
	</div>
</div>
